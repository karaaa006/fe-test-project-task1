<nav class="nav">
    <ul class="nav__list">
        {% for item in pages %}
          <li class="nav__item">
            <a class="nav__link" href={{item.href}}>{{item.title}}</a>
          </li>
        {% endfor %}
    </ul>
</nav>